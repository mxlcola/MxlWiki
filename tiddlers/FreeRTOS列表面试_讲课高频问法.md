## 面试/讲课高频问法（可直接当 Q&A 卡）

1. **Q：为什么要用哨兵结点？**
   A：统一空表与非空表的边界处理，减少分支判断；还能作为“最大值”排序的兜底。
2. **Q：为什么要有 pxIndex？**
   A：支持遍历/轮转调度时的“公平性”，避免新插入结点插队。
3. **Q：pvOwner 和 pxContainer 有什么用？**
   A：`pvOwner` 让你从 item 回到所属对象（如 TCB）；`pxContainer` 让你知道该 item 当前在哪个链表，便于删除/校验。