## 🔍 四、FreeRTOS包含哪些列表

| 列表名                                   | 用途                         | 关键操作       |
| ---------------------------------------- | ---------------------------- | -------------- |
| `pxReadyTasksLists[]`                    | 就绪任务列表（按优先级分类） | vListInsertEnd |
| `xDelayedTaskList1`, `xDelayedTaskList2` | 延时任务列表（按时间排序）   | vListInsert    |
| `xSuspendedTaskList`                     | 挂起任务列表                 | vListInsertEnd |
| `xPendingReadyList`                      | 等待恢复任务列表             | vListInsertEnd |
| `xTimerList`                             | 软件定时器管理               | vListInsert    |

FreeRTOS 通过多个列表灵活地管理任务状态迁移，例如：

- 当任务调用 `vTaskDelay()` 时，会从就绪列表移到延时列表；
- 当延时到期时，再移回就绪列表。

------